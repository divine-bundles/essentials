(nf) README.adoc

#
# Copy runcoms without queueing
#
(prefix: runcoms)
(rno) [^/]+.(ba|z)?sh

(prefix:) (flags:n)
base/.bash_profile
base/.bashrc
base/.zprofile
base/.zshrc

#
# First task: blanks
#
(prefix: blanks) (flags: d)
(+o)  .hushlogin
      .pre.bash
      .pre.zsh
      .pre.sh
      .post.bash
      .post.zsh
      .post.sh